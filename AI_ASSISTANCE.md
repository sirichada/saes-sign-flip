# AI Assistance Disclosure

Claude Code was used throughout this project's extension work — implementing code, running analysis scripts, and drafting written analysis. All AI-assisted code and text were reviewed and verified by the authors against the underlying data/caches before use.

## Reports

Result tables in these reports were generated by script; the written analysis and initial read of the results were drafted with Claude Code assistance:

`amplify_vs_suppress_comparison.md`, `signed_reanalysis.md`, `dead_salmon.md`, `reconstruction_error.md`, `step4_coherence_spotcheck.md`, `degeneracy_metrics.md`, `cebench_crosscheck.md`.

(Report Summaries for `reconstruction_error.md` and `degeneracy_metrics.md` are human-written, per the convention noted in those reports.)

This README (`README.md`) was also drafted by Claude Code.

## Code (`src/`)

The following scripts were implemented with Claude Code assistance: `output_score.py`, `input_score.py`, `sae_utils.py`, `steer.py`, `analyze_suppression_asymmetry.py`, `analyze_reconstruction_error.py`, `analyze_salmon_control.py`, `analyze_generation_degeneracy.py`, `analyze_cebench_candidates.py`, `make_salmon_features.py`, `check_decoder_norms.py`, `cebench_extract.py`.
